{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "app-windows",
  "description": "Minimal capability for robot app windows (load external daemon URLs)",
  "windows": ["app-*"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-destroy"
  ]
}
